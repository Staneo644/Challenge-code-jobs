import { JobSeeker } from '../../applications/job-seekers/job-seeker.entity';
// import { JobId } from 'src/core/jobs/job.entity';

export interface IJobSeekersDomain {
  // createJobSeeker(JobSeekerData: JobSeeker): Promise<JobSeeker | null>;
  // getAllJobSeekers(): Promise<JobSeeker[]>;
  // updateJobSeeker(email: string, JobSeekerData: Partial<JobSeeker>): Promise<JobSeeker | null>;
  // isJobSeeker(email: string): Promise<boolean>;
  // deleteJobSeeker(email: string): Promise<JobSeeker>;
  // getJobSeeker(email: string): Promise<JobSeeker | null>;
  // addJob(JobId: string, email: string): Promise<JobSeeker | null>;
  // getJobs(email: string): Promise<JobId[] | null>;
}
